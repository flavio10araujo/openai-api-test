import { Fragment } from "react";
import Head from "next/head";

function HomePage(props) {
  return (
    <Fragment>
      <Head>
        <title>Buddy TM!</title>
        <meta name="description" content="Buddy TM!" />
      </Head>
    </Fragment>
  );
}

export default HomePage;
